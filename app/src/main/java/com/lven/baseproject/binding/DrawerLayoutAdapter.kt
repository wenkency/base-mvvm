package com.lven.baseproject.binding

/**
 * 自定义一些功能，也可以参考:TextViewBindingAdapter
 */
object DrawerLayoutAdapter {
    /*@JvmStatic
    @BindingAdapter("app:isOpenDrawer", requireAll = false)
    fun isOpenDrawer(drawerLayout: DrawerLayout, isOpen: Boolean) {
        if (isOpen) {
            drawerLayout.openDrawer(GravityCompat.START)
        } else {
            drawerLayout.closeDrawer(GravityCompat.START)
        }
    }*/
}